# Utility scripts

- **destroy-zfs-snapshots.sh**

  This script destroys all snapshots created by Sanoid, including those that have ZFS holds.

- **list-zfs-holds.sh**

  This script lists all holds on snapshots that match the pattern.

- **release-zfs-holds.sh**

  This script releases any existing holds on snapshots that match the pattern.
